// ─── 合入说明：替换 AppFlowController.EnsureGameplayNetworkAsync 整个方法体为：
//
//        async UniTask EnsureGameplayNetworkAsync(SessionMode mode)
//        {
//            await GameAct.Les.LesNetworkGate.EnsureAsync(_net, _steam, mode, _loading);
//        }
//
// Bootstrap ManualBootstrap 中：
//   var net = new GameAct.Les.LesNetworkHub();
//   if (steam.IsInitialized) net.SetUserName(steam.PersonaName);
//   代替 new LiteNetSession();
//
// 完整替换文件见同目录 Bootstrap 与下方 GameplayRunner。
